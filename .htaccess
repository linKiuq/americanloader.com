<IfModule mod_rewrite.c>
    RewriteEngine On

    # Hostinger installs this repository in public_html; Laravel serves public/.
    RewriteRule ^public(?:/|$) - [L]
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
